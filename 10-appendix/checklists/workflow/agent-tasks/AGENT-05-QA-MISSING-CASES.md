# AGENT-05: E2E QA ISSUES & MISSING TEST CASES

## 📋 Overview
Report auto-generated by Senior QA Agent following the completion of the core Shopping & Fulfillment (COD) flows. This document logs discovered bugs (UI/UX) during E2E testing and catalogs the missing test cases that must be verified against `ecommerce-platform-flows.md`.

---

## 🚩 PENDING ISSUES (Discovered during Auto QA)

### 1. [UI/UX] Admin Fulfillment Quantity Not Reacting Real-time
- **Description**: During the Admin Fulfillment progression (Picklist generation -> Picking -> Packing -> Shipped) for Order `ORD-2603-000015`, the `Quantity Picked` and `Quantity Packed` columns in the Fulfillment Details Modal did not refresh their state dynamically upon button clicks. The backend status transitions successfully (`PICKED`, `PACKED`, `SHIPPED`), and closing/re-opening the modal updates the data, indicating a frontend React Context/State missing dependency during the optimistic update.
- **Priority**: 🔵 P2 (Nice to have - Does not block fulfillment flow).
- **Service**: `admin` frontend dashboard.

---

## 🔍 MISSING TEST CASES (Gap Analysis)
Based on `docs/10-appendix/ecommerce-platform-flows.md`, we have successfully verified **5. Cart & Checkout Flows (COD)** and **9. Fulfillment & Shipping Flows (Admin)**. The following scenarios are unverified/missing and require test coverage:

### 1. Payment Integration Flows (Flow 7)
- [ ] Checkout via Paystack/VNPAY simulator.
- [ ] Verify Webhook callbacks for asynchronous payment statuses.
- [ ] Test Payment timeout / Cancellation rollback saga.

### 2. Promo & Discount Engine (Flow 4)
- [ ] Apply percentage-based Coupon Code at checkout.
- [ ] Apply fixed-amount Coupon Code at checkout.
- [ ] Verify free-shipping threshold trigger.

### 3. Returns & Refunds (Flow 10)
- [ ] Initiate a Return request for a `SHIPPED` / `DELIVERED` order via the Customer portal.
- [ ] Admin approval process for Return.
- [ ] Refund triggering when item is received back to inventory.

### 4. Order Cancellations (Flow 6)
- [ ] Customer cancels order before `PROCESSING` / `SHIPPED` status.
- [ ] Admin cancels order and restocks inventory.

### 5. Seller / Merchant Flows (Flow 12 - if applicable to platform tier)
- [ ] Multi-vendor split fulfillment (one order, two items from different warehouses).

### 6. Notifications Verification (Flow 11)
- [ ] Verify Order Confirmation email sent to MailHog or SES.
- [ ] Verify Shipping notification pushed/emailed.

---

## ✅ RESOLVED / FIXED (From Previous Sessions)
- **[Fixed] Database 500 Error during ReserveStock**: Polymorphic relations mapped correctly.
- **[Fixed] Gateway Routing for Analytics/Notifications**: `gateway.yaml` resource mappings updated + duplicated key unmarshaling crash resolved via gitops redeployment.
